<?php

use Krystal\Form\Element;

?>

<h2 class="page-header"><? $this->show('Room types'); ?> <small>(<?= count($types); ?>)</small></h2>

<div class="row">
    <div class="col-lg-6">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>#</th>
                    <th><?php $this->show('Name'); ?></th>
                    <th><?php $this->show('Daily price'); ?></th>
                    <th><?php $this->show('Actions'); ?></th>
                </tr>
            </thead>

            <tbody>
                <?php foreach ($types as $type): ?>
                <tr class="<?= $type['id'] == $id ? 'success' : null; ?>">
                    <td><?= $type['id']; ?></td>
                    <td><?= $type['type']; ?></td>
                    <td><?= number_format($type['unit_price']); ?></td>
                    <td>
                        <a href="<?= $this->url('Site:Architecture:RoomType@editAction', $type['id']); ?>"><i class="glyphicon glyphicon-pencil"></i></a>
                        <a href="<?= $this->url('Site:Architecture:RoomType@deleteAction', $type['id']); ?>"><i class="glyphicon glyphicon-remove"></i></a>
                    </td>
                </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </div>

    <div class="col-lg-6">
        <form class="form-horizontal" action="<?= $this->url('Site:Architecture:RoomType@saveAction'); ?>">
          <fieldset>
            <legend><?php $this->show(!$entity['id'] ? 'Add new room type' : 'Edit room type'); ?></legend>

            <?= Element::hidden('id', $entity['id']); ?>

            <div class="form-group">
              <label class="col-lg-2 control-label"><?php $this->show('Name'); ?></label>
              <div class="col-lg-10">
                <?= Element::text('type', $entity['type'], array('class' => 'form-control')); ?>
              </div>
            </div>

            <div class="form-group">
              <label class="col-lg-2 control-label"><?php $this->show('Daily price'); ?></label>
              <div class="col-lg-10">
                <?= Element::number('unit_price', $entity['unit_price'], array('class' => 'form-control', 'step' => 'any')); ?>
              </div>
            </div>
            
            <div class="form-group">
              <div class="col-lg-10 col-lg-offset-2">
                <button type="submit" class="btn btn-primary"><?php $this->show('Submit'); ?></button>
                <a href="<?= $this->url('Site:Architecture:Grid@indexAction'); ?>" class="btn btn-danger"><?php $this->show('Cancel'); ?></a>
              </div>
            </div>
          </fieldset>
        </form>
    </div>
</div>
